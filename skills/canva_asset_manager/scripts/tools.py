import json, jsonschema\n\n# Funktionale Tools für Parsing, Validierung und TSV-Erzeugung\n\ndef parse_mixed_content(input_data):\n    return {\"text_items\": input_data.get(\"text_items\", []), \"image_items\": input_data.get(\"image_items\", [])}\n\ndef validate_canva_spec(spec, schema_path=\"references/schema/canva_spec.schema.json\"):\n    with open(schema_path) as f: schema = json.load(f)\n    jsonschema.validate(instance=spec, schema=schema)\n    print(\"✅ Schema validiert.\")\n\ndef map_content_to_tsv_rows(slot_model, content):\n    return [{s['id']: 'Beispielinhalt' for s in slot_model}]\n